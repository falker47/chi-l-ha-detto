# Server only. Never prefix database credentials with VITE_.
DATABASE_URL=postgresql://chi_l_ha_detto_app:PASSWORD@HOST.eu-central-1.aws.neon.tech/chi_l_ha_detto?sslmode=require
# Archived export utility only; not used by the app. Do not resume the completed migration:
SUPABASE_URL=https://PROJECT.supabase.co
SUPABASE_EXPORT_KEY=YOUR_SERVER_ONLY_KEY_WITH_SELECT_ACCESS
